<%# The Content Heading %>
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>
    
<%# if (errors) { %>
  <!-- <ul class="notice">
 <%# errors.array().forEach(error => { %>
   <li><%#= error.msg %></li>
<%#  }) %>
 </ul> -->
<%# } %>

<%- messages() %>
<a href="add-classification">Add New Classification</a>
<a href="add-inventory">Add New Inventory Item</a>